{
  "name": "gomoku-server",
  "version": "1.0.0",
  "scripts": {
    "prestart": "coffee --bare --compile public/client.coffee",
    "start": "coffee server.coffee"
  },
  "dependencies": {
    "coffee-script": "^1.12.4",
    "express": "^4.14.1",
    "socket.io": "^2.4.0"
  }
}
